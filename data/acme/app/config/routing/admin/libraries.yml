app_admin_libraries:
    resource: |
        alias: app.libraries
        section: admin
        templates: SyliusAdminBundle:Crud
        redirect: update
        grid: app_admin_libraries
        vars:
            all:
                subheader: app.ui.libraries
            index:
                icon: 'file image outline'
    type: sylius.resource